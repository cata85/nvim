# Cata85's Neovim Configuration

## Installation 
MacOS: `git clone into ~/.config directory`

## Commands
* Leader key is Space.
* This is a work in progress but all macros will be documented and can be seen by using `<leader>MM`.
